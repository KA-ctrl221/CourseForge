FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir flask gunicorn google-cloud-firestore
COPY server.py .
# Cloud Run injects PORT; gunicorn binds to it
CMD exec gunicorn --bind "0.0.0.0:${PORT:-8080}" --workers 1 --threads 8 --timeout 0 server:app
